<main
  class='container text-center d-flex flex-column justify-content-center'
>
    <div class='row'>
        {{#each tracks}}
            {{> trackspartial}}
        {{/each}}
    </div>
</main>

    